<% if @task.errors.blank? %>
  location.href = "<%= session.delete(:return_to)  %>";
<% else %>
  $("#taskNewOrEdit").html("<%= escape_javascript(render 'form') %>");
  $(".modal-backdrop").remove();
  $("#adminModalForm").modal('show');
<% end %>